<p id="notice"><%= notice %></p>

<h3>User Details:<h3>

<table>
  <tr>
    <td>Name: </td>
    <td><%= @user.name %></td>
  </tr>
  <tr>
    <td>Email: </td>
    <td><%= @user.email %></td>
  </tr>
  <tr>
    <td><%= link_to "Products", products_path(user_id: @user.id) %></td>
  </tr>

</table>

<% if @user.id == current_user.id %>
  <%= link_to 'Edit', edit_user_path(@user) %> |
<% end %>
<%= link_to 'Back', users_path %>
